cat > package.json << 'EOF'
{
  "name": "notifications-backend",
  "version": "1.0.0",
  "description": "Backend server for AI push notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "firebase-admin": "^11.11.0",
    "web-push": "^3.6.7",
    "dotenv": "^16.3.1"
  }
}
EOF
